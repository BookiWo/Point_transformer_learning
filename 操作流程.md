# Point Transformer + PartNet 项目操作流程（新手可执行）

> 目标：从 0 开始完成一个 **可训练、可评估、可分析** 的 PyTorch 点云分割项目，严格按“paper → code → experiment → analysis”推进。

---

## 0. 项目总览（你最终要交付什么）

在本项目目录下，最终交付以下内容：

1. 可复现的训练与测试脚本（`scripts/` + 根目录 `train.py/eval.py`）
2. 数据处理与读取模块（`datasets/`）
3. Point Transformer 模型实现（`models/`）
4. 损失函数与指标计算（`losses/` + `utils/metrics/`）
5. 实验配置与日志（`configs/` + `logs/` + `checkpoints/`）
6. 结果可视化与分析报告（`outputs/viz/` + `outputs/reports/`）

---

## 1. 目录职责（先明确“每个文件夹干什么”）

- `datasets/raw/`：你已下载的 PartNet 原始数据（不改动）
- `datasets/processed/`：预处理后可直接训练的数据
- `datasets/splits/`：train/val/test 划分文件
- `models/blocks/`：Point Transformer 的基础模块（attention、邻域聚合）
- `models/backbones/`：编码器/解码器主干网络
- `models/heads/`：分割头（每点分类）
- `losses/`：交叉熵、可选 Dice/Focal
- `utils/metrics/`：mIoU、Acc、per-class IoU
- `utils/visualization/`：点云预测结果上色与导出
- `configs/`：实验参数 yaml（模型、训练、数据）
- `scripts/`：运行入口（训练/评估/bash）
- `checkpoints/`：模型权重
- `logs/`：训练日志（txt/tensorboard）
- `outputs/preds/`：预测结果
- `outputs/viz/`：可视化
- `outputs/reports/`：实验分析文档
- `experiments/exp_pt_partnet_baseline/`：一次完整实验的记录

---

## 2. 分阶段执行清单（按顺序做）

> 建议按 7 个阶段推进。每个阶段都有“完成标准”，达到标准再进入下一阶段。

### 阶段 A：环境与最小可运行框架（Day 1）

**任务**
- 建立 conda/venv 环境
- 安装 PyTorch、numpy、pyyaml、tqdm、scikit-learn、matplotlib、open3d（可选）
- 写最小入口文件：`train.py`、`eval.py`（先只打印参数）

**完成标准**
- 能执行 `python train.py --help`
- 能读取 yaml 配置并打印

---

### 阶段 B：数据预处理与 DataLoader（Day 1-2）

**任务**
- 编写 `tools/preprocess_partnet.py`
- 将原始样本整理为统一结构（点坐标 + 语义/部件标签 + 类别）
- 固定点数（例如 2048/4096）与归一化
- 生成 `datasets/splits/train.txt val.txt test.txt`
- 编写 `datasets/partnet_dataset.py`

**完成标准**
- `DataLoader` 能迭代输出：
  - `points`: `[B, N, 3]`
  - `labels`: `[B, N]`
  - `category`: `[B]`
- 随机可视化 5 个样本，标签和点数正确

---

### 阶段 C：模型基础模块（Day 2-3）

**任务**
- 在 `models/blocks/` 实现：
  - KNN 邻域索引
  - 相对位置编码
  - Point Transformer Attention Layer
- 实现残差 Block（LayerNorm/BN + MLP）

**完成标准**
- 对随机输入前向不报错
- 输入 `[B, N, C]` 输出维度与设计一致

---

### 阶段 D：分割网络组装（Day 3-4）

**任务**
- 在 `models/backbones/` 实现 encoder-decoder（可先简化）
- 在 `models/heads/` 实现每点分类头
- 完成 `models/point_transformer_seg.py`（整网入口）

**完成标准**
- 完整模型 forward 通过
- 输出 logits 形状 `[B, N, num_parts]`
- 参数量统计合理（非 0，且不会异常大）

---

### 阶段 E：训练与验证闭环（Day 4-5）

**任务**
- 在 `losses/` 实现 `CrossEntropyLoss` 包装
- 在 `utils/metrics/` 实现 Acc/mIoU
- 完成 `train.py`：训练循环、验证、保存 best checkpoint
- 完成日志记录（每 epoch 打印 loss/acc/miou）

**推荐初始超参（baseline）**
- Optimizer: AdamW
- lr: 1e-3
- weight_decay: 1e-4
- scheduler: CosineAnnealingLR
- epochs: 200
- batch_size: 8~16（按显存调整）

**完成标准**
- 能完整训练至少 5~10 个 epoch
- `checkpoints/` 产出 best 模型
- 验证集指标呈上升趋势（哪怕较慢）

---

### 阶段 F：测试、可视化与误差分析（Day 5-6）

**任务**
- `eval.py` 支持加载指定 checkpoint
- 保存预测到 `outputs/preds/`
- 将预测结果上色可视化到 `outputs/viz/`
- 统计 per-class IoU / per-category IoU

**完成标准**
- 输出总表（OA、mIoU、各类别 IoU）
- 至少展示 10 个样本的预测可视化

---

### 阶段 G：论文对齐与报告（Day 6-7）

**任务**
- 对齐论文关键设置（邻域大小、层数、特征维）
- 做 2~3 个消融实验：
  - 不同 K（16/32）
  - 不同点数（2048/4096）
  - 不同学习率策略
- 在 `outputs/reports/` 写结果分析

**完成标准**
- 有一份结构化报告：
  1) 实验设置
  2) 主结果
  3) 消融
  4) 失败案例
  5) 下一步优化

---

## 3. 每次编码的固定工作流（建议你一直照这个流程）

1. **先写配置，再写代码**：先在 `configs/` 定义参数，避免硬编码。
2. **先单测模块，再接总线**：每写完一个模块先用随机张量测试。
3. **先跑 1 batch overfit**：确保 loss 能下降，再开长训。
4. **每次只改一个变量**：便于定位问题（例如只改 lr，不同时改 batch size）。
5. **每天保存实验记录**：把参数、结果、现象写入 `experiments/exp_pt_partnet_baseline/`。

---

## 4. 质量门槛（避免“跑了但没学会”）

你至少要能独立回答这些问题：

- PartNet 的标签体系如何映射到 `num_parts`？
- Point Transformer 中 attention 的 key/query/value 来自什么特征？
- 为什么点云任务需要相对位置编码？
- mIoU 与 OA 的差别是什么？为什么 mIoU 更关键？
- 你的训练曲线若震荡，优先调哪 3 个超参？

如果以上问题回答不清，说明还没真正掌握，需要回到相应阶段补课。

---

## 5. 第一周推荐日程（最稳妥）

- Day 1：环境 + 项目脚手架 + 配置系统
- Day 2：数据预处理 + Dataset + 可视化检查
- Day 3：Point Transformer block 实现与单测
- Day 4：整网拼装 + 前向检查
- Day 5：训练闭环 + 指标计算
- Day 6：测试 + 可视化 + 错误分析
- Day 7：消融实验 + 报告整理

---

## 6. 下一步（你现在就做）

按顺序执行：

1. 先在 `configs/` 创建 `partnet_pt_baseline.yaml`
2. 再在 `datasets/` 写 `partnet_dataset.py`
3. 写 `tools/preprocess_partnet.py` 并先跑 20 个样本做验证
4. 确认 DataLoader 输出维度后，再开始写模型

> 只要你愿意，我下一步可以直接在当前目录继续帮你生成：
> - `configs/partnet_pt_baseline.yaml`
> - `datasets/partnet_dataset.py`
> - `tools/preprocess_partnet.py`
> 三个最关键的起步文件（带可运行骨架）。