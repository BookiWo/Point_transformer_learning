# Point Transformer PartNet 数据预处理实施手册（初学者版）

## 1. 这份手册解决什么问题

你现在最需要的是：

1. 明确“预处理到底要做什么”。
2. 明确“处理后应该得到哪些文件”。
3. 明确“这些文件怎么被 `Dataset`、训练、评估脚本使用”。
4. 明确“怎么验证处理结果是对的”。

这份手册不写具体代码，但会把流程讲到可以直接照着实现。

---

## 2. 你当前数据现状（已确认）

项目里已有原始数据（位于 `datasets/raw/shapeNet/hdf5_data`）：

- 训练集列表：`train_hdf5_file_list.txt`
  - `ply_data_train0.h5` ... `ply_data_train5.h5`  
- 验证集列表：`val_hdf5_file_list.txt`
  - `ply_data_val0.h5`
- 测试集列表：`test_hdf5_file_list.txt`
  - `ply_data_test0.h5`, `ply_data_test1.h5`
- 类别与标签映射：
  - `all_object_categories.txt`（16 个大类）
  - `catid_partid_to_overallid.json`（局部 part id -> 全局 50 类 part id）
  - `overallid_to_catid_partid.json`（全局 50 类 part id -> 对应类别+局部 part id）

结论：你可以直接从 HDF5 读数据进行训练，也可以先做“统一预处理”再训练。建议先做统一预处理，后续更清晰、可控、可复现。

---

## 3. 预处理的目标（先定标准，再动手）

预处理不是“把文件改个格式”，而是把原始数据变成训练可直接消费的数据规范。

建议你把目标定成这 5 条：

1. 每个样本都统一为固定点数（例如 2048）。
2. 每个样本坐标都做同样的归一化（避免尺度差异影响训练）。
3. 标签语义统一（明确使用全局 50 part id，还是按类别局部 part id）。
4. 训练/验证/测试划分可追踪、可复现（固定随机种子）。
5. 最终产物可以被 `datasets/partnet_dataset.py` 一次性读取。

---

## 4. 推荐的处理结果目录（你最终应该得到什么）

建议产物组织如下：

- `datasets/processed/partnet_pt/`
  - `samples/`
    - `train/`
    - `val/`
    - `test/`
  - `meta/`
    - `label_mapping.json`
    - `category_mapping.json`
    - `preprocess_config.json`
    - `stats.json`
- `datasets/splits/`
  - `train.txt`
  - `val.txt`
  - `test.txt`

说明：

- `samples/*` 中每个文件对应一个样本（建议一个样本一个文件，便于调试与定位）。
- `splits/*.txt` 中每行写一个样本 ID 或相对路径。
- `meta/preprocess_config.json` 必须记录你当次预处理参数，保证未来可复现。

---

## 5. 单样本字段规范（接口核心）

无论你保存成 `.npz` 还是 `.pt`，建议每个样本至少有以下字段：

1. `points`
   - 含义：点云坐标
   - 形状：`[N, 3]`
   - 类型：`float32`
2. `seg_labels`
   - 含义：每个点的 part 标签
   - 形状：`[N]`
   - 类型：`int64`
3. `category_id`
   - 含义：该样本所属物体类别（16 类之一）
   - 形状：标量
   - 类型：`int64`
4. `sample_id`
   - 含义：样本唯一 ID（字符串）
   - 作用：日志、报错定位、可视化回查

可选字段（后续常用）：

- `points_normal`：法向量（如果原始数据有法向）
- `center` / `scale`：归一化时的中心与缩放因子（反归一化可视化会用到）

---

## 6. 标签体系怎么定（最容易踩坑的点）

ShapeNet Part 常见两种标签使用方式：

1. 全局标签（0~49，共 50 个 part）
2. 类内局部标签（每个大类有自己的 part id 范围，如 2~6 个）

建议初学者第一版使用“全局 50 类标签”，因为：

- 指标实现更直接（统一 `num_parts=50`）。
- 训练输出头固定维度，代码简单。
- 与 `catid_partid_to_overallid.json` 配套明确。

你要在 `meta/label_mapping.json` 里写清楚这件事，避免后面训练和评估口径不一致。

---

## 7. 预处理操作流程（一步一步照做）

### 步骤 1：确定配置参数

先写一个“预处理配置”（可在 `configs/` 下另存），至少包含：

- 输入目录：`datasets/raw/shapeNet/hdf5_data`
- 输出目录：`datasets/processed/partnet_pt`
- 点数：`num_points`（建议先 2048）
- 采样方式：随机采样 / 最远点采样（FPS）
- 是否归一化：是
- 归一化方式：中心化 + 单位球缩放
- 随机种子：固定（如 42）
- 调试模式：`max_samples`（先 20 做冒烟）

### 步骤 2：读取 split 文件

从这三个文本读取原始文件列表：

- `train_hdf5_file_list.txt`
- `val_hdf5_file_list.txt`
- `test_hdf5_file_list.txt`

然后逐个打开对应的 `.h5`，遍历里面的样本。

### 步骤 3：逐样本标准化处理

对每个样本执行同样流程：

1. 读取坐标、标签、类别。
2. 检查数据合法性：
   - 点数 > 0
   - 标签长度与点数一致
   - 标签值在合法范围
3. 统一采样到 `num_points`：
   - 点数不足：可重复采样补齐
   - 点数过多：下采样
4. 坐标归一化：
   - 平移到中心（均值为 0）
   - 按最大半径缩放到单位球
5. 写出到 `samples/{split}/`。

### 步骤 4：生成 split 索引

处理完成后，生成：

- `datasets/splits/train.txt`
- `datasets/splits/val.txt`
- `datasets/splits/test.txt`

每行一个样本路径或样本 ID，确保与实际文件一一对应。

### 步骤 5：生成元信息与统计

至少生成这几个文件：

- `meta/preprocess_config.json`：本次参数快照
- `meta/stats.json`：样本数、每类样本数、每类点数统计、标签频次
- `meta/category_mapping.json`：类别名与 category_id 映射
- `meta/label_mapping.json`：标签体系说明（全局 50 类）

---

## 8. 与其他模块的接口约定（重点）

### 8.1 与 `datasets/partnet_dataset.py` 的接口

`Dataset` 读取约定：

1. 从 `datasets/splits/{split}.txt` 读样本列表。
2. 按路径加载单样本文件。
3. 返回统一字典结构（建议）：
   - `points`: `[N, 3]`
   - `labels`: `[N]`
   - `category`: `[]` 或标量
   - `sample_id`: 字符串

`DataLoader` 组 batch 后目标形状：

- `points`: `[B, N, 3]`
- `labels`: `[B, N]`
- `category`: `[B]`

### 8.2 与训练脚本 `train.py` 的接口

训练脚本假设：

1. `num_parts` 在配置中固定（若全局标签，通常是 50）。
2. `ignore_index`（如果有）与标签值不冲突。
3. 数据增强在 `Dataset` 或 transform 层进行，不在预处理阶段硬编码。

### 8.3 与评估脚本 `eval.py` 的接口

评估脚本会依赖：

1. 与训练同样的标签定义。
2. `sample_id` 用于保存预测结果和可视化对齐。
3. 类别信息 `category` 用于 category-level mIoU 统计。

### 8.4 与可视化模块的接口

如果你要把预测上色保存到 `outputs/viz/`，需要：

1. 预测标签与 GT 标签同一映射体系。
2. 最好保留原始或可反归一化坐标信息。
3. 每个可视化文件名含 `sample_id`，便于回查。

---

## 9. 如何判断“预处理成功”

至少做 4 级验收：

1. 文件级验收
   - `processed` 下样本数量与 split 列表一致。
   - 三个 split 文件都非空。
2. 结构级验收
   - 随机抽查 10 个样本，字段齐全，维度正确。
3. 统计级验收
   - `stats.json` 中每类样本数合理，无明显缺失类别。
4. 训练级验收
   - 能跑通 1 个 batch 前向与 loss 计算。
   - 做 20~100 step 小训练，loss 有下降趋势。

---

## 10. 初学者最常见错误与排查

1. 标签体系混用
   - 现象：loss 不降、IoU 异常低。
   - 排查：确认训练标签和评估标签映射完全一致。
2. 归一化不一致
   - 现象：验证集表现很差。
   - 排查：确保 train/val/test 使用同一归一化逻辑。
3. split 泄漏
   - 现象：验证分数异常高但泛化差。
   - 排查：检查同一样本是否同时出现在 train 和 val/test。
4. 文件索引错位
   - 现象：读取到错误标签或类别。
   - 排查：样本保存顺序、ID、split 索引是否一致。
5. 数据类型错误
   - 现象：训练报错或精度异常。
   - 排查：`points=float32`，`labels=int64`。

---

## 11. 推荐你现在就执行的落地顺序

1. 先写 `tools/preprocess_partnet.py` 的参数与流程框架（不追求复杂）。
2. 先开 `--max_samples 20` 做冒烟处理。
3. 用一个最小版 `datasets/partnet_dataset.py` 读这 20 个样本并打印 batch 维度。
4. 验证通过后，再全量处理 train/val/test。
5. 全量处理后冻结一版 `meta/preprocess_config.json`，后续训练都沿用。

---

## 12. 交付清单模板（你可按此汇报）

完成预处理后，你可以按以下格式记录到实验日志：

1. 输入数据来源
   - 目录：`datasets/raw/shapeNet/hdf5_data`
   - split 文件：train/val/test 列表
2. 预处理参数
   - `num_points`、采样方式、归一化方式、随机种子
3. 输出产物
   - `datasets/processed/partnet_pt/samples/*`
   - `datasets/splits/*.txt`
   - `datasets/processed/partnet_pt/meta/*.json`
4. 验收结果
   - 样本数统计
   - 维度检查通过
   - 1 batch 前向通过
5. 风险与后续
   - 是否需要改为 FPS
   - 是否启用 normals
   - 是否加入更强数据增强

---

## 13. 一句话总结

你要把“原始 HDF5 数据”转成“统一字段、统一标签、统一 split、可复现配置”的标准训练输入；只要你先保证接口稳定，再做模型和训练，后面会轻松很多。